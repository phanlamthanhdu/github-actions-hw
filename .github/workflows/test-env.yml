name: Test environment

on:
  workflow_dispatch:

jobs:
  build-infra:
    needs: prepare
    strategy:
      matrix:
        envs: ["dev", "sit", "prod"]
    uses: ./.github/workflows/call_apply.yml
    with: 
      workspace: ${{matrix.envs}}